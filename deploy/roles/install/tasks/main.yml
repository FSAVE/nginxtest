---
- name: Install EPEL release for nginx
  yum:
    name: epel-release
    state: present

- name: Install nginx web server
  yum:
    name: nginx
    state: latest
    update_cache: true
  become: yes
  remote_user: root
  notify:
    - start nginx